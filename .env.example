# Copy this file to .env.local and paste your key after the = sign.
# .env.local is gitignored and is read automatically by `npm run dev` and `npm start`,
# so you never have to type the key again. NEVER commit a real key.
ANTHROPIC_API_KEY=sk-ant-your-key-here
